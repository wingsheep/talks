{
  "name": "talks",
  "private": true,
  "scripts": {
    "dev": "pnpm run -C 2022-07-10/src dev",
    "build": "rimraf dist && pnpm -r run build"
  },
  "dependencies": {
    "pinia": "^2.0.14",
    "pinia-plugin-persistedstate": "^1.6.1"
  },
  "devDependencies": {
    "@iconify/json": "^2.0.25",
    "@slidev/cli": "^0.28.5",
    "@slidev/theme-default": "^0.21.1",
    "@slidev/theme-seriph": "*",
    "pnpm": "^6.30.0",
    "rimraf": "^3.0.2"
  }
}
